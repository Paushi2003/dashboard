import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.admindashboard.app',
  appName: 'admin-dashboard',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
